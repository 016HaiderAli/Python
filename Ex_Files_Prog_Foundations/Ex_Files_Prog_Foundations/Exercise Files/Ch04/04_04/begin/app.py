miles = input('Enter a distance in miles: ')
# kilometers_value = miles_value * 1.609344

# Converting the value we obtained into kilometers, lets see how its done
kilometers_value = float(miles) * 1.609344

print("The valued obtained after converting the distance into kilometers is ",
      float(kilometers_value))
